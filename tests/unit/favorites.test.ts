import { test, describe, it, expect } from "vitest";
import { getDietClass } from "../../src/pages/favorites";






describe("basic test", () => {
  it("works", () => {
    expect(1 + 1).toBe(2);
  });
});